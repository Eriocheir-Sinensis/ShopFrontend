const styles = theme => ({
  container: {
    paddingTop: theme.spacing(8),
  },
  orderCard: {
    marginBottom: theme.spacing(1),
  },
  meCard: {
    dispay: 'flex',
    alignItems: 'center',
  },
  progress: {
    paddingTop: theme.spacing(8),
    margin: theme.spacing(2),
  }
});

export default styles;
