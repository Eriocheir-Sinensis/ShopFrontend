const styles = theme => ({
  root: {
    paddingTop: theme.spacing(8),
    dispay: 'flex',
    alignItems: 'center',
  },
  progress: {
    paddingTop: theme.spacing(8),
    margin: theme.spacing(2),
  }
});

export default styles;
